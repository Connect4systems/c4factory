frappe.ui.form.on('Pick List', {
    refresh: function(frm) {
        // Placeholder – extend later if you want extra behavior
        // Currently we rely on standard ERPNext Pick List from Work Order.
    }
});
